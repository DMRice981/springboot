server:
  port: 8081 # ??????

spring:
  application:
    name: springboot
  # ???????
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shop_mall?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false
    username: root
    password: ywr988

# MyBatis-Plus
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.mybatisplus.entity
  configuration:
    map-underscore-to-camel-case: true # ????????
  global-config:
    db-config:
      id-type: auto # ??????

