
# Aran Shop 电商系统 - 课程设计完整总结

## 项目概述

Aran Shop 是一套功能完善的电商平台，采用前后端分离架构开发。本项目作为数据库原理及应用课程设计，深入实践了数据库设计、SQL优化、事务处理、触发器等核心技术。

### 项目仓库

- **前端项目**：https://github.com/DMRice981/shop-aranweb
- **后端项目**：（位于当前目录）
- **前端技术栈**：Vue 3 + Vue Router + Axios
- **后端技术栈**：Spring Boot 3.2.5 + MyBatis Plus 3.5.6 + MySQL 8.0

---

## 系统架构

### 整体架构图

```
┌────────────────────────────────────────────────────────────┐
│                     用户端 (Browser)                        │
│              Vue 3 + Vue Router + Axios                     │
└─────────────────────────┬──────────────────────────────────┘
                          │ HTTP/REST API
                          │ http://localhost:8081/api
┌─────────────────────────▼──────────────────────────────────┐
│                  后端服务 (Spring Boot)                      │
│  Controller → Service → Mapper → MyBatis Plus              │
│  端口：8081                                                   │
└─────────────────────────┬──────────────────────────────────┘
                          │ JDBC
┌─────────────────────────▼──────────────────────────────────┐
│                  数据库层 (MySQL 8.0)                       │
│  shop_mall 数据库                                              │
│  • 13张核心表                                                  │
│  • 10个数据库触发器                                            │
│  • 索引优化                                                   │
└────────────────────────────────────────────────────────────┘
```

### 前后端交互

前端项目通过 Axios 封装 HTTP 请求，与后端 REST API 进行通信：

```javascript
// src/utils/request.js
// 基础配置
const service = axios.create({
  baseURL: 'http://localhost:8081/api',
  timeout: 10000
})
```

后端提供统一的 RESTful API 接口，前端按模块组织 API 调用：

```javascript
// 用户相关 API
GET    /api/user/list           - 获取用户列表
POST   /api/user/register       - 用户注册
POST   /api/user/login          - 用户登录

// 商品相关 API
GET    /api/goods/list          - 获取商品列表
GET    /api/goods/{id}          - 获取商品详情
POST   /api/goods               - 添加商品
PUT    /api/goods/{id}          - 更新商品

// 订单相关 API
POST   /api/order/create        - 创建订单
GET    /api/order/list          - 获取订单列表
POST   /api/order/{id}/pay      - 支付订单
POST   /api/order/{id}/ship     - 发货
POST   /api/order/{id}/confirm  - 确认收货
```

---

## 前端项目详解

### 项目结构

```
shop-aranweb/                    # 前端项目
├── src/
│   ├── api/                    # API 接口模块
│   │   ├── admin.js           # 管理员接口
│   │   ├── banner.js           # 轮播图接口
│   │   ├── cart.js            # 购物车接口
│   │   ├── category.js         # 分类接口
│   │   ├── goods.js            # 商品接口
│   │   ├── goodsComment.js     # 评论接口
│   │   ├── goodsImg.js        # 商品图片接口
│   │   ├── order.js            # 订单接口
│   │   ├── orderItem.js        # 订单项接口
│   │   ├── user.js             # 用户接口
│   │   └── userAddress.js      # 地址接口
│   ├── assets/                 # 静态资源
│   │   ├── base.css           # 基础样式
│   │   └── main.css           # 主样式
│   ├── router/                 # 路由配置
│   │   └── index.js           # 路由定义
│   ├── utils/                 # 工具函数
│   │   └── request.js         # Axios 封装
│   ├── views/                 # 页面组件
│   │   ├── 用户端页面
│   │   ├── 管理端页面
│   │   └── 商家端页面
│   ├── App.vue                # 根组件
│   └── main.js                # 入口文件
├── public/                    # 公共资源
├── package.json              # 项目依赖
└── vite.config.js            # Vite 配置
```

### 页面结构

#### 用户端页面 (src/views/)

| 页面文件 | 功能描述 | 路由路径 |
|---------|---------|---------|
| Index.vue | 首页：轮播图、分类导航、商品列表 | / |
| Login.vue | 用户登录页面 | /login |
| Register.vue | 用户注册页面 | /register |
| GoodsDetail.vue | 商品详情页 | /goods/:id |
| Cart.vue | 购物车页面 | /cart |
| Checkout.vue | 结算页面 | /checkout |
| Order.vue | 订单管理页面 | /order |
| Address.vue | 收货地址管理 | /address |
| AfterSale.vue | 售后申请页面 | /after-sale |
| Comment.vue | 商品评论页面 | /comment |
| User.vue | 个人中心页面 | /user |

#### 管理员端页面 (src/views/admin/)

| 页面文件 | 功能描述 | 路由路径 |
|---------|---------|---------|
| AdminLogin.vue | 管理员登录 | /admin/login |
| AdminIndex.vue | 管理后台首页 | /admin |
| UserManage.vue | 用户管理 | /admin/user |
| GoodsManage.vue | 商品管理 | /admin/goods |
| CategoryManage.vue | 分类管理（树形结构） | /admin/category |
| OrderManage.vue | 订单管理 | /admin/order |
| BannerManage.vue | 轮播图管理 | /admin/banner |

#### 商家端页面 (src/views/seller/)

| 页面文件 | 功能描述 | 路由路径 |
|---------|---------|---------|
| SellerLogin.vue | 商家登录 | /seller/login |
| SellerRegister.vue | 商家注册 | /seller/register |
| SellerIndex.vue | 商家后台首页 | /seller |
| SellerGoods.vue | 商品管理 | /seller/goods |
| SellerAfterSale.vue | 售后处理 | /seller/after-sale |

### 路由配置

```javascript
// src/router/index.js
const routes = [
  // 用户端路由
  { path: '/', component: Index },
  { path: '/login', component: Login },
  { path: '/register', component: Register },
  { path: '/goods/:id', component: GoodsDetail },
  { path: '/cart', component: Cart },
  { path: '/checkout', component: Checkout },
  { path: '/order', component: Order },
  { path: '/address', component: Address },
  { path: '/after-sale', component: AfterSale },
  { path: '/user', component: User },
  
  // 管理员路由
  { path: '/admin/login', component: AdminLogin },
  { path: '/admin', component: AdminIndex },
  { path: '/admin/user', component: UserManage },
  { path: '/admin/goods', component: GoodsManage },
  { path: '/admin/category', component: CategoryManage },
  { path: '/admin/order', component: OrderManage },
  { path: '/admin/banner', component: BannerManage },
  
  // 商家路由
  { path: '/seller/login', component: SellerLogin },
  { path: '/seller/register', component: SellerRegister },
  { path: '/seller', component: SellerIndex },
  { path: '/seller/goods', component: SellerGoods },
  { path: '/seller/after-sale', component: SellerAfterSale }
]
```

### API 接口模块

#### 用户模块 (src/api/user.js)

```javascript
import request from '@/utils/request'

export function login(data) {
  return request({
    url: '/user/login',
    method: 'post',
    data
  })
}

export function register(data) {
  return request({
    url: '/user/register',
    method: 'post',
    data
  })
}

export function getUserInfo(userId) {
  return request({
    url: `/user/${userId}`,
    method: 'get'
  })
}

export function updateUser(data) {
  return request({
    url: `/user/${data.id}`,
    method: 'put',
    data
  })
}
```

#### 商品模块 (src/api/goods.js)

```javascript
import request from '@/utils/request'

export function getGoodsList(params) {
  return request({
    url: '/goods/list',
    method: 'get',
    params
  })
}

export function getGoodsDetail(id) {
  return request({
    url: `/goods/${id}`,
    method: 'get'
  })
}

export function addGoods(data) {
  return request({
    url: '/goods',
    method: 'post',
    data
  })
}

export function updateGoods(data) {
  return request({
    url: `/goods/${data.id}`,
    method: 'put',
    data
  })
}
```

### 核心技术特点

1. **Vue 3 Composition API**
   - 使用 `<script setup>` 语法糖
   - 更好的 TypeScript 支持
   - 更灵活的逻辑复用

2. **Vue Router 路由管理**
   - 动态路由参数
   - 路由守卫（登录验证）
   - 嵌套路由

3. **Axios HTTP 客户端**
   - 请求拦截器（添加 Token）
   - 响应拦截器（统一错误处理）
   - 请求超时处理

4. **响应式设计**
   - 适配多种屏幕尺寸
   - 移动端优先设计

---

## 后端项目详解

### 项目结构

```
springboot/                      # 后端项目
├── src/
│   ├── main/
│   │   ├── java/com/mybatisplus/
│   │   │   ├── common/         # 公共类
│   │   │   │   ├── Constants.java    # 常量类
│   │   │   │   └── Result.java       # 统一响应
│   │   │   ├── config/         # 配置类
│   │   │   │   ├── CorsConfig.java   # 跨域配置
│   │   │   │   ├── DotenvConfig.java # 环境变量
│   │   │   │   ├── JacksonConfig.java # 时间格式
│   │   │   │   └── WebMvcConfig.java # Web配置
│   │   │   ├── controller/     # 控制器层
│   │   │   │   ├── AdminController.java
│   │   │   │   ├── UserController.java
│   │   │   │   ├── GoodsController.java
│   │   │   │   ├── OrderController.java
│   │   │   │   ├── CartController.java
│   │   │   │   └── ... (13个控制器)
│   │   │   ├── dto/           # 数据传输对象
│   │   │   │   ├── OrderDTO.java
│   │   │   │   └── AfterSaleDTO.java
│   │   │   ├── entity/        # 实体类 (13个)
│   │   │   ├── mapper/        # 数据访问层 (13个)
│   │   │   └── service/       # 服务层
│   │   │       ├── AdminService.java
│   │   │       ├── impl/
│   │   │       └── ... (13个服务)
│   │   └── resources/
│   │       └── application.yml  # 配置文件
```

### 核心技术特点

1. **Spring Boot 3.x**
   - 自动配置
   - 起步依赖
   - 内嵌 Tomcat

2. **MyBatis Plus 3.5.6**
   - 通用 CRUD 操作
   - Lambda 查询
   - 自动填充

3. **构造器注入**
   - 使用 `@RequiredArgsConstructor`
   - 依赖不可变
   - 易于测试

4. **统一响应格式**

```java
// com.baomidouplus.common.Result
{
  "code": 200,        // 状态码
  "message": "操作成功", // 消息
  "data": {}          // 数据
}
```

---

## 数据库设计详解

### 13张核心数据表

| 序号 | 表名 | 说明 | 关键字段 |
|------|------|------|---------|
| 1 | user | 用户表 | username, password, phone, email |
| 2 | seller | 商家表 | username, shop_name, phone |
| 3 | admin | 管理员表 | admin_name, password, nickname |
| 4 | goods | 商品表 | goods_name, price, stock, sales |
| 5 | category | 分类表 | name, pid（树形结构） |
| 6 | `order` | 订单表 | order_no, total_price, order_status |
| 7 | order_item | 订单项表 | order_no, goods_id, num |
| 8 | cart | 购物车表 | user_id, goods_id, num |
| 9 | user_address | 收货地址表 | user_id, name, phone, address |
| 10 | goods_comment | 商品评论表 | goods_id, user_id, score, content |
| 11 | goods_img | 商品图片表 | goods_id, img_url, sort |
| 12 | banner | 轮播图表 | img_url, link_url, sort |
| 13 | after_sale | 售后表 | order_id, reason, status |

### 10个数据库触发器

#### 1. 订单支付后自动扣库存

```sql
CREATE TRIGGER trg_after_order_paid
AFTER UPDATE ON `order`
FOR EACH ROW
BEGIN
    IF OLD.pay_status = 0 AND NEW.pay_status = 1 THEN
        UPDATE goods g
        INNER JOIN order_item oi ON g.id = oi.goods_id
        SET g.stock = g.stock - oi.num,
            g.sales = g.sales + oi.num
        WHERE oi.order_no = NEW.order_no;
    END IF;
END
```

#### 2. 订单创建时间

```sql
CREATE TRIGGER trg_before_order_insert
BEFORE INSERT ON `order`
FOR EACH ROW
BEGIN
    IF NEW.create_time IS NULL THEN
        SET NEW.create_time = NOW();
    END IF;
END
```

#### 3-10. 其他触发器

- `trg_before_order_pay_time` - 订单支付时间
- `trg_before_order_send_time` - 订单发货时间
- `trg_before_order_confirm_time` - 订单确认时间
- `trg_before_order_item_insert` - 订单项创建时间
- `trg_before_goods_insert` - 商品创建时间
- `trg_before_goods_update` - 商品更新时间
- `trg_before_cart_insert` - 购物车添加时间
- `trg_before_cart_update` - 购物车更新时间

### 数据库设计亮点

1. **树形分类结构**
   - 使用 `pid` 字段实现父子分类
   - 支持无限层级分类

2. **软删除机制**
   - 使用 `is_delete` 字段
   - 数据可恢复
   - 历史数据可追溯

3. **索引优化**
   - 用户名唯一索引
   - 商品分类索引
   - 订单号唯一索引

4. **反范式设计**
   - 订单项冗余存储商品信息
   - 保证历史订单准确性

---

## 核心功能实现

### 订单创建流程

```
用户选择商品
    ↓
加入购物车 或 直接购买
    ↓
进入结算页面
    ↓
选择收货地址
    ↓
提交订单 (POST /api/order/create)
    ↓
后端处理：
  1. 生成唯一订单号
  2. 检查商品库存
  3. 计算订单总价
  4. 创建订单记录
  5. 创建订单项记录
  6. 预扣减库存
    ↓
返回订单信息
```

### 订单状态流转

```
0: 待付款 → 1: 待发货 → 2: 已发货 → 3: 已完成
    ↓              ↓            ↓
    └──────── 4: 已取消 ←────────┘
```

### 库存管理机制

**创建订单时**：预扣减库存（订单项已确定，但未支付）

**支付成功时**：触发器自动扣减库存，增加销量

**取消订单时**：回滚库存（如果已支付）

---

## 系统演示场景

### 用户端功能演示

1. **注册登录**
   - 用户注册 → 填写信息 → 注册成功
   - 用户登录 → 输入账号密码 → 登录成功

2. **商品浏览**
   - 首页轮播图展示
   - 分类导航切换
   - 商品列表展示
   - 商品详情查看
   - 商品评论查看

3. **购物流程**
   - 加入购物车 → 查看购物车 → 修改数量 → 删除商品
   - 结算页面 → 选择地址 → 确认订单
   - 支付订单 → 等待发货

4. **订单管理**
   - 查看订单列表
   - 查看订单详情
   - 支付订单
   - 确认收货
   - 申请售后
   - 评价商品

### 商家端功能演示

1. **商品管理**
   - 添加商品 → 填写信息 → 上架
   - 编辑商品信息
   - 下架商品

2. **订单处理**
   - 查看订单列表
   - 订单发货操作

3. **售后处理**
   - 查看售后申请
   - 处理售后请求

### 管理员端功能演示

1. **分类管理**
   - 添加分类
   - 编辑分类
   - 树形结构展示
   - 删除分类

2. **商品管理**
   - 查看所有商品
   - 上架/下架商品

3. **用户管理**
   - 查看用户列表
   - 禁用/启用用户

4. **订单管理**
   - 查看所有订单
   - 查看订单详情

5. **轮播图管理**
   - 添加轮播图
   - 编辑轮播图
   - 删除轮播图

---

## 项目亮点总结

### 1. 技术栈先进

- **前端**：Vue 3 + Vite + Axios
- **后端**：Spring Boot 3.2.5 + MyBatis Plus 3.5.6
- **数据库**：MySQL 8.0

### 2. 架构设计优秀

- 前后端分离架构
- RESTful API 设计
- 分层架构（Controller → Service → Mapper）

### 3. 数据库设计专业

- 13张核心表设计
- 10个触发器保证数据一致性
- 树形分类结构
- 软删除机制
- 索引优化

### 4. 功能完善

- 3个角色（用户、商家、管理员）
- 完整的电商业务流程
- 购物车、订单、售后
- 商品管理、分类管理

### 5. 代码质量高

- 常量统一管理
- 统一响应格式
- 构造器注入
- 事务管理

---

## 课程设计材料清单

| 材料名称 | 文件位置 | 说明 |
|---------|---------|------|
| 后端源代码 | `/workspace/src/` | Spring Boot 项目 |
| 前端源代码 | GitHub仓库 | Vue 3 项目 |
| 数据库脚本 | `database_init.sql` | 13张表 + 测试数据 |
| 触发器脚本 | `database_triggers.sql` | 10个触发器 |
| 课程设计报告 | `课程设计报告.md` | 约10000字 |
| PPT大纲 | `答辩PPT大纲.md` | 17页详细大纲 |
| 项目说明 | `README.md` | 快速开始指南 |
| 开发指南 | `DEVELOPMENT_GUIDE.md` | 详细开发文档 |

---

## 快速启动指南

### 1. 初始化数据库

```bash
# 创建数据库并导入数据
mysql -u root -p < database_init.sql

# 导入触发器
mysql -u root -p shop_mall < database_triggers.sql
```

### 2. 启动后端服务

```bash
# 配置数据库连接
cp .env.example .env
# 编辑 .env 文件配置数据库信息

# 启动后端
mvn spring-boot:run
# 后端服务：http://localhost:8081
```

### 3. 启动前端服务

```bash
# 克隆前端项目
git clone https://github.com/DMRice981/shop-aranweb.git
cd shop-aranweb

# 安装依赖
npm install

# 启动开发服务器
npm run dev
# 前端服务：http://localhost:5173
```

### 4. 测试账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | 123456 |
| 商家 | seller1 | 123456 |
| 用户 | user1 | 123456 |

---

## 项目截图说明

建议在答辩PPT中添加以下系统截图：

1. **首页截图**
   - 轮播图展示
   - 分类导航
   - 商品列表

2. **商品详情截图**
   - 商品图片
   - 价格信息
   - 商品描述
   - 购买按钮

3. **购物车截图**
   - 商品列表
   - 数量调整
   - 总价计算

4. **订单流程截图**
   - 订单列表
   - 订单详情
   - 状态流转

5. **管理后台截图**
   - 分类管理（树形结构）
   - 商品管理
   - 用户管理

6. **商家后台截图**
   - 商品管理
   - 订单处理
   - 售后处理

---

## 答辩准备建议

### 1. PPT制作要点

- **第1-2页**：封面 + 项目概述（2分钟）
- **第3-4页**：需求分析 + 系统架构（2分钟）
- **第5-6页**：数据库设计 ER图 + 表结构（3分钟）
- **第7-9页**：核心功能实现 + 代码展示（3分钟）
- **第10-12页**：系统演示截图（3分钟）
- **第13-14页**：项目亮点 + 遇到的问题（2分钟）
- **第15-16页**：总结与展望（1分钟）

### 2. 代码展示准备

建议展示以下核心代码：

1. **数据库触发器** - 库存自动扣减
2. **订单创建接口** - OrderController.create()
3. **订单支付接口** - OrderController.pay()
4. **前端API调用** - src/api/order.js
5. **路由配置** - src/router/index.js

### 3. 常见问题准备

1. **为什么使用触发器？**
   - 保证数据一致性
   - 减少业务代码复杂度
   - 原子操作避免并发问题

2. **如何保证库存不超卖？**
   - 订单创建时检查库存
   - 使用事务保证原子性
   - 触发器在支付时扣减库存

3. **数据库设计遵循什么范式？**
   - 第三范式（3NF）
   - 合理的反范式设计（订单项冗余存储）

4. **前后端如何交互？**
   - RESTful API
   - Axios HTTP 客户端
   - JSON 数据格式

---

## 总结

Aran Shop 电商系统是一套功能完善、技术先进、代码规范的课程设计作品。通过本次课程设计，我们：

1. ✅ 掌握了数据库设计的核心原则
2. ✅ 实践了 SQL 触发器的应用
3. ✅ 学会了前后端分离架构的开发
4. ✅ 理解了事务和并发控制
5. ✅ 提升了软件工程实践能力

祝答辩顺利！🎉

---

**项目作者**：DMRice981  
**前端仓库**：https://github.com/DMRice981/shop-aranweb  
**完成时间**：2026年5月
